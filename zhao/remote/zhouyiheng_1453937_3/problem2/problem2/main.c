#include <stdio.h>

int main(){
	/*
		Assume (x = 0Xff33, MASK = 0xFF00). Expression: c = (x & MASK) == 0; output: c:1 x = 0Xff33, MASK = 0xFF00
		Assume (x = 10, y = 2, z = 2). Expression: z = y = (x++) + (++y)*2; output: z:15 y:15 x:11
		Assume (x = 10, y = 4, z = 1). Expression: y >>= (x & 0x2) && z;  output: 
	*/
	return 0;
}